# 南極スライダー 点検記録（GIGA Standard v4）

2026-08-03 時点。数字はすべて**実測**。測っていないものは「未計測」と書く。

計測環境：Chromium 141（Playwright）、820×1180 / DPR 2。
`npm run build` は不要（静的配信）なので、リポジトリの中身をそのまま
`/Ice_slide-puzzle/` 以下に置いて測った。

---

## 1. まとめ

| 区分 | 前 | 後 |
|---|---|---|
| コントラスト基準未満 | **6件** | **0件** |
| タップ44px未満 | **4件** | **0件** |
| 画像の合計 | **1321.2 KB** | **44.1 KB**（-96.7%） |
| 拡大の禁止 | `user-scalable=no` あり | **外した** |
| CSP | 無し | 有り（インライン `<script>` を外部化） |
| `offline.html` | 無し | 有り（圏外で白画面にならない） |
| 更新の通知 | 無し（黙って入れ替わる） | 押すまで切り替わらない |
| 初回訪問の再読み込み | — | 1回（正常。§4 参照） |
| ホーム画面に追加 | 案内なし | ボタンを出す |
| manifest | `id`/`scope` が相対、項目不足 | 絶対パス＋不足分を追加 |
| LICENSE / dependabot | 無し | 追加 |

---

## 2. 画像が 1.3MB あった

`favicon.png` が **922KB**（1024×1024 のフルカラー PNG）。
タブとブックマークに出すためだけの画像としては桁が違う。

| ファイル | 前 | 後 |
|---|---:|---:|
| `favicon.png` | 922.5 KB | **5.9 KB** |
| `icons/icon-512.png` | 183.6 KB | 14.3 KB |
| `icons/maskable-512.png` | 124.5 KB | 13.2 KB |
| `icons/icon-192.png` | 36.9 KB | 3.9 KB |
| `icons/apple-touch-icon.png` | 29.3 KB | 3.9 KB |
| `icons/maskable-192.png` | 24.4 KB | 3.0 KB |
| **合計** | **1321.2 KB** | **44.1 KB** |

色数の少ない絵なのでフルカラーで持つ必要がない。パレット PNG にした。
`favicon.png` は 256×256 に落とした。**40人が同時に開く校内 Wi-Fi では、
この差がそのまま初回表示の待ち時間になる。**

maskable アイコンは**実測でセーフゾーン外 0.00%**（＝正しく作られている）だったので、
大きさだけ落として中身には触っていない。手順は `tools/optimize-icons.mjs`。

---

## 3. 拡大を禁止していた

```html
<meta name="viewport" content="... maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
```

**見えづらい子が画面を大きくできない。** 外した。

盤面は `main` に `touch-action: none` があるので、指でなぞる操作はそちらで守られている。
**拡大を許しても遊びにくくならない**（実測：コマ10個、ターン表示とも正常）。

---

## 4. 初回訪問で画面が1回勝手に読み込み直されていた

これは**この改修で自分が入れた不具合**で、実測して気づいた。

`controllerchange` を「切り替えが済んだ合図」として素直に受けると、
**はじめて開いたときにも飛んでくる**（`activate` の `clients.claim()` で
ページが管理下に入るため）。結果、初回訪問が必ず1回リロードされる。

**ゲームでは、並べたばかりの盤面がその場でリセットされる。**

```
初回訪問での画面遷移回数: 2   （1 が正常）
```

「もともと管理下だったか」で分ける直し方も試したが、これは別の形で壊れる。
**入れた直後に更新を押した場合、切り替わったのに読み込み直されなくなった。**

見るべきは**利用者が押したかどうか**だけだった。

```js
let userAskedUpdate = false;
navigator.serviceWorker.addEventListener('controllerchange', () => {
  if (!userAskedUpdate || reloading) return;
  reloading = true;
  window.location.reload();
});
```

直したあとの実測。

```
初回訪問での画面遷移回数: 1
押すまで切り替わらない: 3秒放置で waiting のまま / 勝手な再読み込み 0
押したら切り替わる:     再読み込みされた / 古いキャッシュが消えた / 案内も消えた
```

---

## 5. CSP を入れたら、ゲームが動かなくなった

`script-src 'self'` で締めた直後、**盤面が「準備中」のまま止まった**。

```
駒の数: 0 / ターン表示: null
```

原因は、ゲーム本体が `index.html` の中にインラインで書かれていたことと、
ボタンが `onclick="initGame()"` だったこと。どちらも `'self'` では実行されない。

**`'unsafe-inline'` を足せば直るが、それでは CSP を入れた意味がほとんど無くなる。**
440行を `app.js` に切り出し、`onclick=` を `addEventListener` に繋ぎ替えた。
中身は動かしていない。

直したあとの実測：駒の数 10 / ターン表示「ばん」／JS エラー 0。

> **CSP は「入れた」だけでは終わらない。入れたあとに動かして確かめること。**
> ビルドも静的解析も通るので、動かさないと気づけない。

---

## 6. コントラスト：6件 → 0件

| 対象 | 色 | 比 | 直し方 |
|---|---|---:|---|
| ヘルプボタン `？` | 白 on `#ffca28` | **1.53** | 文字を濃い茶 `#4a3600` に |
| あそびかたの番号 | 白 on `#ffca28` | **1.53** | 同上 |
| `.rule-title` | `#1a73e8` | 4.27 | `--primary-color` を `#1967d2` に |
| ヘッダーのアプリ名 | `#1a73e8` | 4.27 | 同上 |
| ふりがな `なんきょく` | `#666` | 4.44 | `#5f6368` に |
| ターン表示 | `#00C2CB` | 2.19 | **コマの色は変えず**、文字用に `#007a80` を用意 |

黄色の上の白文字（**1.53**）が最も読めなかった。
面の色（ブランド色）は変えず、文字だけ落としている。

ターン表示は、コマの色をそのまま文字に使っていた。
**コマの色を変えるとプレイヤーの見分けがつかなくなる**ので、
同じ色相の濃い版を文字用に用意した。

`--primary-color: #1a73e8`（Google Blue 600）は白地で 4.27。
`#1967d2`（Blue 700）にすると 5.0 になる。色の印象はほとんど変わらない。

---

## 7. タップ領域：4件 → 0件

| 対象 | 前 | 直し方 |
|---|---|---|
| ヘッダーの丸ボタン×3 | 40×40 | 円は 40px のまま、疑似要素で当たり判定だけ 44px に |
| フッターのリンク | 35×11 | 同上 |
| SweetAlert2 のボタン | 116×41 | `min-height: 44px` |

**見た目は変えていない。** ボタン自体を大きくすると、ヘッダーが詰まって折り返す。

---

## 8. 他アプリのキャッシュを巻き添えにしないこと

`sw.js` はすでに接頭辞で絞る形だったが、**本当に効いているか**を実測した。
他アプリのキャッシュを2つ置いてから版を上げ、`activate` を通した結果。

```json
{
  "全部": ["keisan-card-static-1.0.0", "nankyoku-precache-v9",
           "nankyoku-runtime-v9", "shukudai-post-v3"],
  "keisan-card 残った": true,
  "宿題ポスト 残った": true
}
```

自アプリの古い版だけが消えている。

---

## 9. そのほか

- **manifest**：`id`/`start_url`/`scope` が `"./"` だった。解決結果は同じだが、
  表記を `/Ice_slide-puzzle/` に統一した。**同一性は変わらない**（＝すでに
  インストールしてある端末で別アプリ扱いにはならない）。
  `dir` / `display_override` / `launch_handler` / `categories` を追加。
- **`offline.html`**：外部資産にも JavaScript にも一切頼らないページを追加し、
  先読みに入れた。本体のキャッシュだけ消して圏外にすると、正しく出ることを確認済み。
- **ホーム画面に追加**：`beforeinstallprompt` を `<head>` の先頭で受け取る
  `install-hook.js` を追加し、案内できるときだけボタンを出す。
- **フォント**：Google Fonts が塞がれても崩れないよう、端末側の日本語フォントを
  後ろに並べた。この作業環境では実際に塞がれているため、
  **フィルタリングされた学校と同じ状態で全画面を測っている。**
- **SweetAlert2** はすでに `vendor/` に自己ホスト済み（CDN 依存なし）。

---

## 10. 未計測・未対応

- 対戦を最後まで進めた画面（勝敗の表示）は未計測。
- 実機（Chromebook / iPad）での確認は未実施。
- `favicon.png` の元画像（1024×1024）はリポジトリに残していない。
  作り直すときは、より大きい元絵から `tools/optimize-icons.mjs` を通すこと。

---

## 11. 測り直す手順

```bash
npm ci
npm run icons     # アイコンを作り直す（元絵を差し替えたときだけ）
```

表示の実測（コントラスト・タップ領域・PWA の挙動）は、
リポジトリの中身を `/Ice_slide-puzzle/` 以下に置いて Playwright で行った。
道具は Digital_textbook の
[`scripts/measure/`](https://github.com/GIGAyama/Digital_textbook/tree/main/scripts/measure) にある。

**リリースするときは `sw.js` の `CACHE_VERSION` を必ず1つ上げること。**
上げ忘れると、児童の端末に古い版が残ったままになる。
